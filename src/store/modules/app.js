import Cookies from '@icony/vue-container/js-cookie'

const state = {
  device: 'desktop'
}

const mutations = {
  
}

const actions = {
  
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
